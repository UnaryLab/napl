# napl optimization (speedup) log

Records produced by the `napl-opt-sim` skill - one row per optimization run. Each row reports a
behavior-preserving speedup attempt for one kernel: whether code changed, the correctness gate
(the kernel's `test_<kernel>.py`, plus the downstream sweep when the kernel is a shared primitive),
and the before/after wall-clock on CPU and GPU (MPS) with the measured speedup. **Speedup** is the
kernel's own pre-optimization runtime divided by its optimized runtime on IDENTICAL inputs (GPU
timed with `torch.mps.synchronize()` before stopping the clock); runtimes are per-workload and not
comparable across rows. A `Changed=no` row (no safe speedup found, ~1.00x) is recorded too, so a
known-near-optimal kernel is not re-attempted blindly. **Source file** + **Hash** (the file's
`git hash-object` value after the run) double as the improve idempotency key: the napl-port-unarysim
workflow skips a file whose current hash matches a recorded row and re-optimizes one that differs.

| Date | napl kernel | Source file | Hash | Changed | Test gate | CPU base->opt (speedup) | GPU/MPS base->opt (speedup) | What changed |
|------|-------------|-------------|------|---------|-----------|-------------------------|-----------------------------|--------------|
| 2026-06-13 | algorithm.butterfly_binary | src/napl/algorithm/fft/butterfly.py | 382b157986917f194dc3ed8870f8d8552820ea01 | no | (napl-port-unarysim Improve) |  |  | No changes made; no safe meaningful speedup available within butterfly.py. butterfly_spike.forward is a thin orchestration layer over already-optimized composed primitives (encoder, mul_csg, add_any, decoder), each documenting prior optimization passes (cached input probs, reused int8 views, shape-guarded in-place accumulators preserving the (1,)->(N,) first-call broadcast). A CPU cProfile (5 runs, 256 timesteps, batch 1024) shows the time lives inside those primitives, not butterfly.py's glue: mul.py forward 0.121s and add.py 0.058s of 0.317s total; butterfly.py's own forward body is negligible. The only butterfly-local micro-ops are the four 1 - spike (torch.rsub) calls, but they operate on four distinct operands (no dedup), and rewriting 1 - x to an XOR-with-1 on int8 spikes would be a marginal, dtype-risky change for negligible payoff since the bottleneck is op dispatch count in the composed ops, not the rsub primitive. butterfly_binary is a trivial loop-free elementwise reference (the analytic baseline) with nothing to optimize. Per the skill (if no safe speedup, change nothing) and coding-discipline (surgical, don't force risky edits), left unchanged. Baseline = final timing on identical seeded inputs: CPU 50.9 ms, MPS 265.4 ms (~1.00x). |
| 2026-06-13 | algorithm.butterfly_spike | src/napl/algorithm/fft/butterfly.py | 382b157986917f194dc3ed8870f8d8552820ea01 | no | (napl-port-unarysim Improve) |  |  | No changes made; no safe meaningful speedup available within butterfly.py. butterfly_spike.forward is a thin orchestration layer over already-optimized composed primitives (encoder, mul_csg, add_any, decoder), each documenting prior optimization passes (cached input probs, reused int8 views, shape-guarded in-place accumulators preserving the (1,)->(N,) first-call broadcast). A CPU cProfile (5 runs, 256 timesteps, batch 1024) shows the time lives inside those primitives, not butterfly.py's glue: mul.py forward 0.121s and add.py 0.058s of 0.317s total; butterfly.py's own forward body is negligible. The only butterfly-local micro-ops are the four 1 - spike (torch.rsub) calls, but they operate on four distinct operands (no dedup), and rewriting 1 - x to an XOR-with-1 on int8 spikes would be a marginal, dtype-risky change for negligible payoff since the bottleneck is op dispatch count in the composed ops, not the rsub primitive. butterfly_binary is a trivial loop-free elementwise reference (the analytic baseline) with nothing to optimize. Per the skill (if no safe speedup, change nothing) and coding-discipline (surgical, don't force risky edits), left unchanged. Baseline = final timing on identical seeded inputs: CPU 50.9 ms, MPS 265.4 ms (~1.00x). |
| 2026-06-13 | metric.accuracy | src/napl/metric/accuracy.py | c21fa4aa4d4a3609279806adcfe74fdedda17e01 | yes | (napl-port-unarysim Improve) |  |  | In accuracy.forward() (the per-timestep hot path), replaced the unconditional out-of-place accumulation sc.data = sc.add(spike) with a shape-guarded form: first timestep broadcasts the (1,) seed up to the spike shape out-of-place (sc.data = sc.add(spike)), steady state accumulates in place via sc.add_(spike). This removes one (N,) float32 allocation per timestep for 255 of 256 timesteps. Dtype-safe: float32.add_(int8) preserves the destination's float32 dtype (verified), no truncation/raise. The shape guard preserves the required (1,)->(N,) first-call broadcast of the scalar accumulator seed. Numerics are bit-exact vs the original out-of-place semantics (max \|spike_value - float64 reference\| = 0.0 on both cpu and mps). report_error and the rest of the file were already optimized (aminmax fusion, single abs() reuse) and were left untouched. Timing (median, mirrors test config: 256 timesteps, N=10000, bipolar): cpu 3.03->~2.6 ms (~1.16x), mps 4.76->~4.1 ms (~1.16x). No source dependents need a separate gate: accuracy is a metric consumed only via the standard forward-then-report_error pattern (rnn, butterfly) and by its own test; the change is behavior-preserving for all callers. |
| 2026-06-13 | metric.correlation | src/napl/metric/correlation.py | a2bf1859e35a1b339bd7d1c50cc21cb49ed95383 | yes | (napl-port-unarysim Improve) |  |  | Dropped the two redundant .type(self.ntype) casts on in_1_is_1/in_2_is_1 in the per-timestep forward(). torch.ne(...) returns bool; the downstream addcmul/add against the float32 (ntype) accumulators type-promote bool automatically, so the casts were pure extra GPU/CPU dispatches. Bit-exact (verified torch.equal on paired_11/sum_1/sum_2; accumulators remain float32). Removing 2 of ~7 per-timestep kernel launches roughly halves runtime on the launch-bound MPS path; CPU gains modestly. Left the ne(.,0) nonzero guard, the out-of-place .data scalar-accumulator broadcast, and the autocorrelation delay-buffer cast untouched. CPU 8.24->~7.5ms (~1.10x); MPS 88.07->~43.7ms (~2.01x). |
| 2026-06-13 | metric.stability | src/napl/metric/stability.py | 32ccf8a57bdfb538407a1406e0fbdc0312a90794 | yes | (napl-port-unarysim Improve) |  |  | In stability.forward, changed the per-timestep error computation from an out-of-place subtraction (spike_value - self.source).abs_() to an in-place spike_value.sub_(self.source).abs_(), eliminating one full-N-tensor allocation per timestep. Safe and behavior-preserving: accuracy.forward returns a fresh spike_value tensor each call and overwrites its .data the next call, so mutating it in-place here does not corrupt accuracy's state or self.source; both operands are float (ntype), so the in-place subtraction triggers no dtype promotion, and the operand shapes are equal so there is no scalar->N broadcast to break. Verified bit-exact (torch.equal) against the original out-of-place logic on both cpu and mps. Timing (warmup=2, iters=10, 256 timesteps, N=1000): CPU 5.33->4.55 ms (~1.17x), MPS 19.8->16.3 ms (~1.21x). The inner accuracy.forward (a separate class/file, out of scope) accounts for ~half the remaining cost. |
| 2026-06-13 | module.conv_fsu | src/napl/module/conv.py | 91bbea7b4628de27b42c9e39f44fbf8739785765 | no | (napl-port-unarysim Improve) |  |  | No change made; conv.py is already well-optimized and no safe speedup exists within this file. conv_fsu (the only class with a per-timestep loop, 256 steps): CPU profile shows the hot path is dominated by torch im2col/col2im (unfold/fold), which is intrinsic algorithmic work, not removable without changing the algorithm. The code already (a) uses matmul plus an algebraic bipolar correction instead of materializing the (batch*L,out,K) elementwise spike product, (b) precomputes the rate-0.5 pad bits to plain floats once to avoid a per-timestep device->host sync on GPU, and (c) does one unfold per timestep. Every .type() cast is a genuine int8<->float conversion required because unfold/fold/matmul lack int8 support; I verified add_any returns stype, so the line-275 cast is NOT redundant (my initial assumption that it was a no-op was wrong). The only loop invariant (out_hw via conv2d_output_shape) is negligible Python integer math and does not appear in the profile; caching it would add module state for no measurable gain, against coding-discipline's surgical/simplicity rules. conv_fxp/conv_hub/conv_tlut are single-shot wrappers (sub-millisecond) delegating all real work to the autograd Functions in module/linear.py via _conv2d_binary (im2col + linear_fn + fold); their forward bodies have no loop to vectorize and any real win lives in module/linear.py (out of scope). Baseline timings on identical seeded inputs: conv_fsu cpu 40.9 ms / mps 136.8 ms; conv_fxp cpu 0.47/mps 2.32, conv_hub cpu 0.52/mps 1.61, conv_tlut cpu 1.03/mps 2.18 ms. Reporting ~1.0x. All four classes verified correct on both CPU and MPS (FSU rmse ~0.004 < 0.03; binary rmse 0.009-0.025 < 0.05). |
| 2026-06-13 | module.conv_fsu_pc | src/napl/module/conv_fsu_pc.py | e52426006e654d1ecb88fa613ddc5c973eaa26d0 | no | (napl-port-unarysim Improve) |  |  | No safe, beneficial speedup found; file left unchanged. The hot path is already vectorized: the spike product is a matmul (no (batch*L,out,K) materialization), pad bits are precomputed to floats once to avoid per-step device->host syncs, and the int8->ntype casts are required (unfold/fold lack int8 support). I prototyped caching the per-timestep conv2d_output_shape() call (keyed on the fixed input H/W) to skip host-side Python shape math; measured back-to-back on identical seeded inputs it gave no speedup (within noise, marginally slower on cpu and mps) since the Python shape math is negligible vs unfold/2x matmul/fold, so I reverted it. The bipolar 1-inp / (1-wf).t() allocations depend on freshly-encoded per-timestep operands and cannot be hoisted; an algebraic XNOR-count rewrite would reorder the float reduction and risk changing results, so it was not pursued (behavior-preserving constraint). |
| 2026-06-13 | module.conv_fxp | src/napl/module/conv.py | 91bbea7b4628de27b42c9e39f44fbf8739785765 | no | (napl-port-unarysim Improve) |  |  | No change made; conv.py is already well-optimized and no safe speedup exists within this file. conv_fsu (the only class with a per-timestep loop, 256 steps): CPU profile shows the hot path is dominated by torch im2col/col2im (unfold/fold), which is intrinsic algorithmic work, not removable without changing the algorithm. The code already (a) uses matmul plus an algebraic bipolar correction instead of materializing the (batch*L,out,K) elementwise spike product, (b) precomputes the rate-0.5 pad bits to plain floats once to avoid a per-timestep device->host sync on GPU, and (c) does one unfold per timestep. Every .type() cast is a genuine int8<->float conversion required because unfold/fold/matmul lack int8 support; I verified add_any returns stype, so the line-275 cast is NOT redundant (my initial assumption that it was a no-op was wrong). The only loop invariant (out_hw via conv2d_output_shape) is negligible Python integer math and does not appear in the profile; caching it would add module state for no measurable gain, against coding-discipline's surgical/simplicity rules. conv_fxp/conv_hub/conv_tlut are single-shot wrappers (sub-millisecond) delegating all real work to the autograd Functions in module/linear.py via _conv2d_binary (im2col + linear_fn + fold); their forward bodies have no loop to vectorize and any real win lives in module/linear.py (out of scope). Baseline timings on identical seeded inputs: conv_fsu cpu 40.9 ms / mps 136.8 ms; conv_fxp cpu 0.47/mps 2.32, conv_hub cpu 0.52/mps 1.61, conv_tlut cpu 1.03/mps 2.18 ms. Reporting ~1.0x. All four classes verified correct on both CPU and MPS (FSU rmse ~0.004 < 0.03; binary rmse 0.009-0.025 < 0.05). |
| 2026-06-13 | module.conv_hub | src/napl/module/conv.py | 91bbea7b4628de27b42c9e39f44fbf8739785765 | no | (napl-port-unarysim Improve) |  |  | No change made; conv.py is already well-optimized and no safe speedup exists within this file. conv_fsu (the only class with a per-timestep loop, 256 steps): CPU profile shows the hot path is dominated by torch im2col/col2im (unfold/fold), which is intrinsic algorithmic work, not removable without changing the algorithm. The code already (a) uses matmul plus an algebraic bipolar correction instead of materializing the (batch*L,out,K) elementwise spike product, (b) precomputes the rate-0.5 pad bits to plain floats once to avoid a per-timestep device->host sync on GPU, and (c) does one unfold per timestep. Every .type() cast is a genuine int8<->float conversion required because unfold/fold/matmul lack int8 support; I verified add_any returns stype, so the line-275 cast is NOT redundant (my initial assumption that it was a no-op was wrong). The only loop invariant (out_hw via conv2d_output_shape) is negligible Python integer math and does not appear in the profile; caching it would add module state for no measurable gain, against coding-discipline's surgical/simplicity rules. conv_fxp/conv_hub/conv_tlut are single-shot wrappers (sub-millisecond) delegating all real work to the autograd Functions in module/linear.py via _conv2d_binary (im2col + linear_fn + fold); their forward bodies have no loop to vectorize and any real win lives in module/linear.py (out of scope). Baseline timings on identical seeded inputs: conv_fsu cpu 40.9 ms / mps 136.8 ms; conv_fxp cpu 0.47/mps 2.32, conv_hub cpu 0.52/mps 1.61, conv_tlut cpu 1.03/mps 2.18 ms. Reporting ~1.0x. All four classes verified correct on both CPU and MPS (FSU rmse ~0.004 < 0.03; binary rmse 0.009-0.025 < 0.05). |
| 2026-06-13 | module.conv_tlut | src/napl/module/conv.py | 91bbea7b4628de27b42c9e39f44fbf8739785765 | no | (napl-port-unarysim Improve) |  |  | No change made; conv.py is already well-optimized and no safe speedup exists within this file. conv_fsu (the only class with a per-timestep loop, 256 steps): CPU profile shows the hot path is dominated by torch im2col/col2im (unfold/fold), which is intrinsic algorithmic work, not removable without changing the algorithm. The code already (a) uses matmul plus an algebraic bipolar correction instead of materializing the (batch*L,out,K) elementwise spike product, (b) precomputes the rate-0.5 pad bits to plain floats once to avoid a per-timestep device->host sync on GPU, and (c) does one unfold per timestep. Every .type() cast is a genuine int8<->float conversion required because unfold/fold/matmul lack int8 support; I verified add_any returns stype, so the line-275 cast is NOT redundant (my initial assumption that it was a no-op was wrong). The only loop invariant (out_hw via conv2d_output_shape) is negligible Python integer math and does not appear in the profile; caching it would add module state for no measurable gain, against coding-discipline's surgical/simplicity rules. conv_fxp/conv_hub/conv_tlut are single-shot wrappers (sub-millisecond) delegating all real work to the autograd Functions in module/linear.py via _conv2d_binary (im2col + linear_fn + fold); their forward bodies have no loop to vectorize and any real win lives in module/linear.py (out of scope). Baseline timings on identical seeded inputs: conv_fsu cpu 40.9 ms / mps 136.8 ms; conv_fxp cpu 0.47/mps 2.32, conv_hub cpu 0.52/mps 1.61, conv_tlut cpu 1.03/mps 2.18 ms. Reporting ~1.0x. All four classes verified correct on both CPU and MPS (FSU rmse ~0.004 < 0.03; binary rmse 0.009-0.025 < 0.05). |
| 2026-06-13 | module.decoder | src/napl/module/decoder.py | 752f1793935ce1cb9f1d0b79dd2098f4a666fa22 | yes | (napl-port-unarysim Improve) |  |  | Dropped the per-timestep explicit spike = spike.type(self.ntype) cast in decoder.forward. The accumulation add/add_ already type-promotes its int8 (stype) operand into the float32 (ntype) accumulator, so the explicit full-size cast was a redundant allocation/kernel-launch every timestep. Verified safe: float32.add_(int8) promotes the operand exactly (no truncation, no raise) and float32.add(int8) -> float32 for the first-call (1,)->(N,) broadcast path, so the shape-guarded in-place accumulator and the scalar-broadcast first timestep are both preserved. The accumulator stays float32 and outputs are bit-exact to the pre-edit code. Result (median ms, 10000-wide stream, 1024 timesteps): CPU ~12.65 -> ~11-12.6 ms (flat, within noise: the cast is cheap relative to the div/mul/sub chain on CPU); MPS 28.78 -> ~14-15.6 ms (~1.9x, async GPU benefits most from one fewer full-size kernel launch per tick). The bipolar value transform and the in-place accumulator pattern were already optimal and left unchanged. |
| 2026-06-13 | module.encoder | src/napl/module/encoder.py | c37210e73887d9ba8274293e1c3646a8d9dacdf7 | yes | (napl-port-unarysim Improve) |  |  | Resolved the polarity branch once in __init__ as self._is_bipolar = (self.polarity == 'bipolar') and replaced the two per-timestep string comparisons in forward() (if self.polarity == 'bipolar' / elif self.polarity == 'unipolar') with a boolean test (if self._is_bipolar / else). check_polarity with polarity_required=True guarantees polarity is exactly 'unipolar' or 'bipolar', so the else (unipolar) branch is equivalent to the original elif; numerical outputs are unchanged (bit-exact, max abs err 0.0 on CPU and MPS). The existing _prob_cache bipolar-transform cache is preserved. Removes two Python string compares per timestep call; the dominant per-timestep cost (the (10000,)-element gt+cast, plus MPS kernel-launch overhead) is irreducible without breaking the one-timestep-per-call streaming API. Measured median runtime for 1024 timesteps over a (10000,) input: CPU 6.59 -> ~6.0 ms (~1.10x), MPS 34.74 -> ~30.9 ms (~1.12x); no CUDA on host. Modest, partly within CPU timing noise but consistently non-negative on both devices. |
| 2026-06-13 | module.linear_fsu | src/napl/module/linear.py | 9efdeff9dd640b24d527f93babf7b6bfb14b8c30 | yes | (napl-port-unarysim Improve) |  |  | Optimized linear_fsu_pc.forward bipolar branch: replaced the second matmul torch.matmul(1-xf,(1-wf).t()) (which materialized two full (...,in) and (out,in) one-minus tensors per timestep) with the bit-exact integer identity sum((1-x)(1-w)) = in_features - sum(x) - sum(w) + sum(xw), reusing the already-computed AND-count matmul. This removes one matmul and two tensor allocations from every per-timestep call in the hot streaming loop. Verified bit-exact (torch.equal) for scalar and batched spike inputs before editing. Measured (bipolar workload, median of 15 iters, stable across 3 runs): CPU ~5.54 -> ~4.75 ms (~1.17x), MPS ~45.9 -> ~42.8 ms (~1.07x). linear_fsu's bipolar correction is already algebraic (no second matmul) so it was left unchanged. The single-shot binary-domain kernels linear_fxp/linear_hub/linear_tlut are called once (no per-timestep loop), run sub-ms on CPU, and are MPS-launch-bound; their autograd.Function forwards carry STE/dtype/broadcast contracts, so no safe meaningful speedup exists and they were left unchanged (timing deltas there are run-to-run noise). All hard constraints respected: numerical outputs unchanged (integer identity is exact; tests pass with identical rmse/max_err to baseline), no in-place dtype/broadcast/autograd hazards introduced, lazy operation imports in __init__ untouched, no float >>/<<. |
| 2026-06-13 | module.linear_fsu_pc | src/napl/module/linear.py | 9efdeff9dd640b24d527f93babf7b6bfb14b8c30 | yes | (napl-port-unarysim Improve) |  |  | Optimized linear_fsu_pc.forward bipolar branch: replaced the second matmul torch.matmul(1-xf,(1-wf).t()) (which materialized two full (...,in) and (out,in) one-minus tensors per timestep) with the bit-exact integer identity sum((1-x)(1-w)) = in_features - sum(x) - sum(w) + sum(xw), reusing the already-computed AND-count matmul. This removes one matmul and two tensor allocations from every per-timestep call in the hot streaming loop. Verified bit-exact (torch.equal) for scalar and batched spike inputs before editing. Measured (bipolar workload, median of 15 iters, stable across 3 runs): CPU ~5.54 -> ~4.75 ms (~1.17x), MPS ~45.9 -> ~42.8 ms (~1.07x). linear_fsu's bipolar correction is already algebraic (no second matmul) so it was left unchanged. The single-shot binary-domain kernels linear_fxp/linear_hub/linear_tlut are called once (no per-timestep loop), run sub-ms on CPU, and are MPS-launch-bound; their autograd.Function forwards carry STE/dtype/broadcast contracts, so no safe meaningful speedup exists and they were left unchanged (timing deltas there are run-to-run noise). All hard constraints respected: numerical outputs unchanged (integer identity is exact; tests pass with identical rmse/max_err to baseline), no in-place dtype/broadcast/autograd hazards introduced, lazy operation imports in __init__ untouched, no float >>/<<. |
| 2026-06-13 | module.linear_fxp | src/napl/module/linear.py | 9efdeff9dd640b24d527f93babf7b6bfb14b8c30 | yes | (napl-port-unarysim Improve) |  |  | Optimized linear_fsu_pc.forward bipolar branch: replaced the second matmul torch.matmul(1-xf,(1-wf).t()) (which materialized two full (...,in) and (out,in) one-minus tensors per timestep) with the bit-exact integer identity sum((1-x)(1-w)) = in_features - sum(x) - sum(w) + sum(xw), reusing the already-computed AND-count matmul. This removes one matmul and two tensor allocations from every per-timestep call in the hot streaming loop. Verified bit-exact (torch.equal) for scalar and batched spike inputs before editing. Measured (bipolar workload, median of 15 iters, stable across 3 runs): CPU ~5.54 -> ~4.75 ms (~1.17x), MPS ~45.9 -> ~42.8 ms (~1.07x). linear_fsu's bipolar correction is already algebraic (no second matmul) so it was left unchanged. The single-shot binary-domain kernels linear_fxp/linear_hub/linear_tlut are called once (no per-timestep loop), run sub-ms on CPU, and are MPS-launch-bound; their autograd.Function forwards carry STE/dtype/broadcast contracts, so no safe meaningful speedup exists and they were left unchanged (timing deltas there are run-to-run noise). All hard constraints respected: numerical outputs unchanged (integer identity is exact; tests pass with identical rmse/max_err to baseline), no in-place dtype/broadcast/autograd hazards introduced, lazy operation imports in __init__ untouched, no float >>/<<. |
| 2026-06-13 | module.linear_hub | src/napl/module/linear.py | 9efdeff9dd640b24d527f93babf7b6bfb14b8c30 | yes | (napl-port-unarysim Improve) |  |  | Optimized linear_fsu_pc.forward bipolar branch: replaced the second matmul torch.matmul(1-xf,(1-wf).t()) (which materialized two full (...,in) and (out,in) one-minus tensors per timestep) with the bit-exact integer identity sum((1-x)(1-w)) = in_features - sum(x) - sum(w) + sum(xw), reusing the already-computed AND-count matmul. This removes one matmul and two tensor allocations from every per-timestep call in the hot streaming loop. Verified bit-exact (torch.equal) for scalar and batched spike inputs before editing. Measured (bipolar workload, median of 15 iters, stable across 3 runs): CPU ~5.54 -> ~4.75 ms (~1.17x), MPS ~45.9 -> ~42.8 ms (~1.07x). linear_fsu's bipolar correction is already algebraic (no second matmul) so it was left unchanged. The single-shot binary-domain kernels linear_fxp/linear_hub/linear_tlut are called once (no per-timestep loop), run sub-ms on CPU, and are MPS-launch-bound; their autograd.Function forwards carry STE/dtype/broadcast contracts, so no safe meaningful speedup exists and they were left unchanged (timing deltas there are run-to-run noise). All hard constraints respected: numerical outputs unchanged (integer identity is exact; tests pass with identical rmse/max_err to baseline), no in-place dtype/broadcast/autograd hazards introduced, lazy operation imports in __init__ untouched, no float >>/<<. |
| 2026-06-13 | module.linear_tlut | src/napl/module/linear.py | 9efdeff9dd640b24d527f93babf7b6bfb14b8c30 | yes | (napl-port-unarysim Improve) |  |  | Optimized linear_fsu_pc.forward bipolar branch: replaced the second matmul torch.matmul(1-xf,(1-wf).t()) (which materialized two full (...,in) and (out,in) one-minus tensors per timestep) with the bit-exact integer identity sum((1-x)(1-w)) = in_features - sum(x) - sum(w) + sum(xw), reusing the already-computed AND-count matmul. This removes one matmul and two tensor allocations from every per-timestep call in the hot streaming loop. Verified bit-exact (torch.equal) for scalar and batched spike inputs before editing. Measured (bipolar workload, median of 15 iters, stable across 3 runs): CPU ~5.54 -> ~4.75 ms (~1.17x), MPS ~45.9 -> ~42.8 ms (~1.07x). linear_fsu's bipolar correction is already algebraic (no second matmul) so it was left unchanged. The single-shot binary-domain kernels linear_fxp/linear_hub/linear_tlut are called once (no per-timestep loop), run sub-ms on CPU, and are MPS-launch-bound; their autograd.Function forwards carry STE/dtype/broadcast contracts, so no safe meaningful speedup exists and they were left unchanged (timing deltas there are run-to-run noise). All hard constraints respected: numerical outputs unchanged (integer identity is exact; tests pass with identical rmse/max_err to baseline), no in-place dtype/broadcast/autograd hazards introduced, lazy operation imports in __init__ untouched, no float >>/<<. |
| 2026-06-13 | module.mgu_fsu | src/napl/module/rnn.py | 1049dc484989cb7b0167d83b3a680d2a76bd4de7 | yes | (napl-port-unarysim Improve) |  |  | Optimized mgu_hardfxp.forward by deduplicating redundant round_fxp (self.trunc) quantizations. round_fxp is a pure, deterministic STE function of its input, so t(hx), t(input), t(fg), t(fg_hx), t(ng) -- each previously computed twice in the original forward -- are now each computed once and the result reused across consumers. This is behavior-preserving (reusing one autograd node feeding two consumers is numerically identical to two separate STE nodes: forward values match exactly, backward gradients accumulate identically) and halves those redundant quantization passes. Measured speedup, identical inputs (seed 0), median of repeated runs: CPU 0.196->0.151 ms (~1.30x), MPS 0.62->0.51 ms (~1.20x, noisy at this sub-ms scale but trends faster). Numerical output unchanged: mgu_hardfxp-vs-mgu_hard rmse stayed exactly 0.0101 on both cpu and mps, gradients finite. The other three non-placeholder classes were left unchanged after analysis (no safe speedup found): (1) mgu_hard is a single-shot trainable cell whose intermediates feed autograd, so in-place is forbidden and there is no redundant compute; (2) mgu_fsu is the per-timestep streaming inner cell whose rnn.py-local code (two torch.cat of int8 spikes, an integer 3-operand sum) is already minimal with no dedupable work; (3) mgu_hub's ~36ms runtime is dominated by its 256-cycle loop calling out-of-scope submodules (encoder, linear_fsu, add_any, mul_csg, mul_and, sigmoid_hard, accuracy) which are already optimized and outside this file. mgu_hard 0.96-1.01x and mgu_hub 0.97x are timing noise (unchanged). Kept the lazy operation imports in every __init__; no >>/<< on float tensors introduced; no in-place dtype/broadcast/autograd changes. |
| 2026-06-13 | module.mgu_hard | src/napl/module/rnn.py | 1049dc484989cb7b0167d83b3a680d2a76bd4de7 | yes | (napl-port-unarysim Improve) |  |  | Optimized mgu_hardfxp.forward by deduplicating redundant round_fxp (self.trunc) quantizations. round_fxp is a pure, deterministic STE function of its input, so t(hx), t(input), t(fg), t(fg_hx), t(ng) -- each previously computed twice in the original forward -- are now each computed once and the result reused across consumers. This is behavior-preserving (reusing one autograd node feeding two consumers is numerically identical to two separate STE nodes: forward values match exactly, backward gradients accumulate identically) and halves those redundant quantization passes. Measured speedup, identical inputs (seed 0), median of repeated runs: CPU 0.196->0.151 ms (~1.30x), MPS 0.62->0.51 ms (~1.20x, noisy at this sub-ms scale but trends faster). Numerical output unchanged: mgu_hardfxp-vs-mgu_hard rmse stayed exactly 0.0101 on both cpu and mps, gradients finite. The other three non-placeholder classes were left unchanged after analysis (no safe speedup found): (1) mgu_hard is a single-shot trainable cell whose intermediates feed autograd, so in-place is forbidden and there is no redundant compute; (2) mgu_fsu is the per-timestep streaming inner cell whose rnn.py-local code (two torch.cat of int8 spikes, an integer 3-operand sum) is already minimal with no dedupable work; (3) mgu_hub's ~36ms runtime is dominated by its 256-cycle loop calling out-of-scope submodules (encoder, linear_fsu, add_any, mul_csg, mul_and, sigmoid_hard, accuracy) which are already optimized and outside this file. mgu_hard 0.96-1.01x and mgu_hub 0.97x are timing noise (unchanged). Kept the lazy operation imports in every __init__; no >>/<< on float tensors introduced; no in-place dtype/broadcast/autograd changes. |
| 2026-06-13 | module.mgu_hardfxp | src/napl/module/rnn.py | 1049dc484989cb7b0167d83b3a680d2a76bd4de7 | yes | (napl-port-unarysim Improve) |  |  | Optimized mgu_hardfxp.forward by deduplicating redundant round_fxp (self.trunc) quantizations. round_fxp is a pure, deterministic STE function of its input, so t(hx), t(input), t(fg), t(fg_hx), t(ng) -- each previously computed twice in the original forward -- are now each computed once and the result reused across consumers. This is behavior-preserving (reusing one autograd node feeding two consumers is numerically identical to two separate STE nodes: forward values match exactly, backward gradients accumulate identically) and halves those redundant quantization passes. Measured speedup, identical inputs (seed 0), median of repeated runs: CPU 0.196->0.151 ms (~1.30x), MPS 0.62->0.51 ms (~1.20x, noisy at this sub-ms scale but trends faster). Numerical output unchanged: mgu_hardfxp-vs-mgu_hard rmse stayed exactly 0.0101 on both cpu and mps, gradients finite. The other three non-placeholder classes were left unchanged after analysis (no safe speedup found): (1) mgu_hard is a single-shot trainable cell whose intermediates feed autograd, so in-place is forbidden and there is no redundant compute; (2) mgu_fsu is the per-timestep streaming inner cell whose rnn.py-local code (two torch.cat of int8 spikes, an integer 3-operand sum) is already minimal with no dedupable work; (3) mgu_hub's ~36ms runtime is dominated by its 256-cycle loop calling out-of-scope submodules (encoder, linear_fsu, add_any, mul_csg, mul_and, sigmoid_hard, accuracy) which are already optimized and outside this file. mgu_hard 0.96-1.01x and mgu_hub 0.97x are timing noise (unchanged). Kept the lazy operation imports in every __init__; no >>/<< on float tensors introduced; no in-place dtype/broadcast/autograd changes. |
| 2026-06-13 | module.mgu_hub | src/napl/module/rnn.py | 1049dc484989cb7b0167d83b3a680d2a76bd4de7 | yes | (napl-port-unarysim Improve) |  |  | Optimized mgu_hardfxp.forward by deduplicating redundant round_fxp (self.trunc) quantizations. round_fxp is a pure, deterministic STE function of its input, so t(hx), t(input), t(fg), t(fg_hx), t(ng) -- each previously computed twice in the original forward -- are now each computed once and the result reused across consumers. This is behavior-preserving (reusing one autograd node feeding two consumers is numerically identical to two separate STE nodes: forward values match exactly, backward gradients accumulate identically) and halves those redundant quantization passes. Measured speedup, identical inputs (seed 0), median of repeated runs: CPU 0.196->0.151 ms (~1.30x), MPS 0.62->0.51 ms (~1.20x, noisy at this sub-ms scale but trends faster). Numerical output unchanged: mgu_hardfxp-vs-mgu_hard rmse stayed exactly 0.0101 on both cpu and mps, gradients finite. The other three non-placeholder classes were left unchanged after analysis (no safe speedup found): (1) mgu_hard is a single-shot trainable cell whose intermediates feed autograd, so in-place is forbidden and there is no redundant compute; (2) mgu_fsu is the per-timestep streaming inner cell whose rnn.py-local code (two torch.cat of int8 spikes, an integer 3-operand sum) is already minimal with no dedupable work; (3) mgu_hub's ~36ms runtime is dominated by its 256-cycle loop calling out-of-scope submodules (encoder, linear_fsu, add_any, mul_csg, mul_and, sigmoid_hard, accuracy) which are already optimized and outside this file. mgu_hard 0.96-1.01x and mgu_hub 0.97x are timing noise (unchanged). Kept the lazy operation imports in every __init__; no >>/<< on float tensors introduced; no in-place dtype/broadcast/autograd changes. |
| 2026-06-13 | operation.add_any | src/napl/operation/add.py | 9477de3c4cd3a438069bbbc440648a12515643cc | yes | (napl-port-unarysim Improve) |  |  | add_any.forward (dim!=None hot path): replaced acc_delta = torch.sum(input, dim, dtype=ntype) - self.offset (which allocates a full-size partial AND a second full-size tensor for the offset subtraction) with an in-place sub on the freshly-allocated, op-owned sum result: acc_delta = torch.sum(...); acc_delta.sub_(self.offset). This removes one full-size float allocation per timestep while computing the identical (partial - offset) value in the identical order, so the subsequent accumulator.add_(acc_delta).clamp_(...) math is unchanged. The (1,)->(N,) first-call broadcast guard and the addcmul_ fusion were left intact. The dim is None path was deliberately NOT touched: there input.type(ntype) can alias the caller's input when input is already ntype, so an in-place sub would be unsafe (and it is not the test's hot path). Rationale: the spike-reduction is inherently per-timestep in the streaming paradigm and cannot be vectorized across timesteps from inside the op; the only safe win is killing redundant allocations, which matters most on GPU where each alloc is a separate kernel launch. Behavior-preserving verified bit-exact (torch.equal on every timestep output) against the original allocating formulation on both CPU and MPS. Measured: op-only CPU ~87ms->~76ms (~1.1x); end-to-end MPS ~44.8ms->~34-38ms (~1.2-1.3x); end-to-end CPU is encoder-dominated and within timing noise of baseline (145-180ms vs 156ms baseline). No regression on any device. |
| 2026-06-13 | operation.bi2uni | src/napl/operation/bi2uni.py | 29909c737f078971f97c82735d8b1e52880d9caf | yes | (napl-port-unarysim Improve) |  |  | In bi2uni.forward(), removed the per-timestep input.type(self.ntype) cast (the float32 ntype accumulator already promotes the int8 input via alpha=2, no truncation) and converted the accumulator update from an out-of-place .add to a shape-guarded in-place .add_ once the buffer shape matches the input. This eliminates 2 allocations per timestep in steady state. The first forward() call still uses the out-of-place .add to broadcast the (1,) accumulator up to the (N,) input shape, preserving the scalar-accumulator broadcast contract. Numerics are bit-exact vs the original algorithm on both CPU and MPS (torch.equal over all 256 timesteps). Result: MPS ~31.3->25.0 ms (~1.25x); CPU ~8.4->7.9 ms (flat, within noise). Win is modest because the 256 per-timestep kernel launches dominate over allocation cost; the allocation-sensitive MPS path benefits most. |
| 2026-06-13 | operation.dff | src/napl/operation/dff.py | 1a9fde230684f7705f074b891d1cbb087ac8a491 | no | (napl-port-unarysim Improve) |  |  | No changes made. The dff.forward() hot path is already the optimized circular-buffer-of-references idiom that the skill itself cites as the target pattern: per timestep it does only a list index read, a list assignment, an integer head increment, and a comparison, plus tick()'s int increment. There are zero per-timestep tensor allocations, clones, dtype casts, or torch.roll reallocations in steady state (the only tensor work, the lazy zero-buffer init, happens once on the first call). There is no per-timestep spike reduction to vectorize, no matmul opportunity, and no loop-invariant to hoist. Micro-tweaks considered and rejected as non-wins: replacing the head-advance branch with modulo (no faster for small depth, numerically identical) and removing the per-call is_first_call boolean (negligible, and it anchors lazy device/shape init). Per the skill's 'if there is no safe speedup, change nothing' rule, the file is left untouched and reported at ~1.00x. Baseline timing (256 timesteps, identical seeded inputs): cpu 3.49 ms, mps 14.86 ms. |
| 2026-06-13 | operation.div_cordiv | src/napl/operation/div.py | 6e66eb4411c2aca262a88090ce15f3e6af09aaab | yes | (napl-port-unarysim Improve) |  |  | div_cordiv: in forward(), moved the per-call temporary off self (self.rand_q -> local rand_q) to avoid an nn.Module.__setattr__ each timestep (it runs Parameter/Module/Tensor isinstance checks and was prominent in the profile; rand_q is written and read only within the same call), and dropped the redundant .type(self.stype) on the quotient (torch.where of two stype operands is already stype). Bit-exact vs the pre-edit output (torch.equal) on CPU and MPS. div_iscb: no change - its forward is pure sub-module composition (out-of-scope separate files), and the .type(torch.int8) casts on the XOR line are load-bearing because stype is configurable (float/bfloat16 would break ^). Speedup is within measurement noise (~1.0x): the 256-timestep workload is dominated by torch op dispatch and encoder/decoder/sub-module overhead, not the removed setattr/cast; reported honestly rather than inventing a number. cpu div_cordiv 13.02->~12.9 ms (~1.0x), mps 48.19->~47.6 ms (~1.0x); div_iscb cpu 41.95 ms / mps 138.28 ms unchanged within noise. |
| 2026-06-13 | operation.div_iscb | src/napl/operation/div.py | 6e66eb4411c2aca262a88090ce15f3e6af09aaab | yes | (napl-port-unarysim Improve) |  |  | div_cordiv: in forward(), moved the per-call temporary off self (self.rand_q -> local rand_q) to avoid an nn.Module.__setattr__ each timestep (it runs Parameter/Module/Tensor isinstance checks and was prominent in the profile; rand_q is written and read only within the same call), and dropped the redundant .type(self.stype) on the quotient (torch.where of two stype operands is already stype). Bit-exact vs the pre-edit output (torch.equal) on CPU and MPS. div_iscb: no change - its forward is pure sub-module composition (out-of-scope separate files), and the .type(torch.int8) casts on the XOR line are load-bearing because stype is configurable (float/bfloat16 would break ^). Speedup is within measurement noise (~1.0x): the 256-timestep workload is dominated by torch op dispatch and encoder/decoder/sub-module overhead, not the removed setattr/cast; reported honestly rather than inventing a number. cpu div_cordiv 13.02->~12.9 ms (~1.0x), mps 48.19->~47.6 ms (~1.0x); div_iscb cpu 41.95 ms / mps 138.28 ms unchanged within noise. |
| 2026-06-13 | operation.gt_rc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.jkff | src/napl/operation/jkff.py | 1e310e2bb4714ecde55e60ab4413aeeed55cdd54 | no | (napl-port-unarysim Improve) |  |  | No change. The jkff.forward already uses a single behavior-preserving torch.where select implementing the JK characteristic eq Q'=Q?(NOT K):J (its comment notes it already replaced a 4-mask/3-bitwise form). Benchmarked two bit-exact alternatives (arithmetic select jn+q*(1-kn-jn); int8-branch where) on a 100k-element x 200-timestep workload: current is fastest on CPU (26.4 ms vs arith 37.6, where_int8 53.6). On MPS arith is ~1.15x (15.7 vs 18.1) but regresses CPU to ~0.70x; where_int8 is slower on both. No rewrite wins on both devices, so per the skill's no-safe-speedup rule I changed nothing (~1.0x). Baseline timings on the unchanged file: cpu 22.5 ms, mps 18.0 ms. |
| 2026-06-13 | operation.lt_rc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.max_rc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.max_tc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.min_rc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.min_tc | src/napl/operation/compare.py | 8623963c3cb8e33adc7867f763c0a978f46ff7b8 | yes | (napl-port-unarysim Improve) |  |  | Hoisted the duplicate per-timestep int8 casts in the four sync_skewed-based rc classes (min_rc, max_rc, lt_rc, gt_rc). Each forward() previously computed sync_0.type(torch.int8) and/or sync_1.type(torch.int8) twice (once for d_enable and again for the and_gate / dff-update term). They are now computed once each into sync_0_i8/sync_1_i8 and reused, removing one int8-cast allocation per timestep per class. This is value-identical (same cast, same result), so outputs are bit-exact. min_tc/max_tc were left unchanged: their only casts (int8 cast before the &/\| gate and the final stype cast) are required for correctness when spike_type is a float dtype, so there is no safe redundant op to remove. Measured speedup is within timing noise because these end-to-end test wirings are dominated by encoder/decoder/sync overhead, not the removed cast (baseline vs optimized, ms, cpu/mps: min_rc 20.6/65.1 -> 20.1/64.0; max_rc 18.5/61.0 -> 20.1/64.1; lt_rc 14.8/53.5 -> 15.9/56.0; gt_rc 14.8/51.7 -> 14.8/52.6; min_tc 6.6/23.8 unchanged; max_tc 7.1/24.3 unchanged). No regression; the edit is a safe behavior-preserving micro-optimization (one fewer allocation/timestep). |
| 2026-06-13 | operation.mul_and | src/napl/operation/mul.py | 10b4e5c93dae6731f3fd5387d4a8459d566d9a67 | yes | (napl-port-unarysim Improve) |  |  | mul_csg: removed the per-timestep in_0_long = input_0.type(torch.long) cast (a redundant 10000-element int8->long allocation each of 1024 timesteps). long.add(int8) type-promotes to long, so the gather-index counters (seq_idx, seq_idx_inv) stay long indices while the add operand is the already-available int8 spike view; the out-of-place .add is preserved to keep the (1,)->(N,) first-call scalar-accumulator broadcast intact. Also computed 1 - in_0_i8 once and reused it for both path_inv and the seq_idx_inv update, dropping a redundant subtraction. Verified bit-exact (torch.equal) against the original forward on identical inputs for BOTH bipolar and unipolar paths on CPU and MPS. Interleaved A/B median timing (same process, identical seeded inputs): cpu 127.86->123.71 ms (1.03x), mps 155.68->137.20 ms (1.14x). The win is modest because runtime is dominated by the two num_seq gathers and gt comparisons, not the removed cast. mul_and was examined and left UNCHANGED: its per-timestep work is trivial bitwise AND/XNOR on already-int8 (stype) spikes where the .type(torch.int8) casts are no-ops, and cost is dominated by encoder/decoder; no safe speedup. Note: mul_csg is a dependency of module/rnn.py (an rnn cell uses it); the edit is bit-exact so downstream behavior is unchanged, to be confirmed by the central sweep gate. |
| 2026-06-13 | operation.mul_csg | src/napl/operation/mul.py | 10b4e5c93dae6731f3fd5387d4a8459d566d9a67 | yes | (napl-port-unarysim Improve) |  |  | mul_csg: removed the per-timestep in_0_long = input_0.type(torch.long) cast (a redundant 10000-element int8->long allocation each of 1024 timesteps). long.add(int8) type-promotes to long, so the gather-index counters (seq_idx, seq_idx_inv) stay long indices while the add operand is the already-available int8 spike view; the out-of-place .add is preserved to keep the (1,)->(N,) first-call scalar-accumulator broadcast intact. Also computed 1 - in_0_i8 once and reused it for both path_inv and the seq_idx_inv update, dropping a redundant subtraction. Verified bit-exact (torch.equal) against the original forward on identical inputs for BOTH bipolar and unipolar paths on CPU and MPS. Interleaved A/B median timing (same process, identical seeded inputs): cpu 127.86->123.71 ms (1.03x), mps 155.68->137.20 ms (1.14x). The win is modest because runtime is dominated by the two num_seq gathers and gt comparisons, not the removed cast. mul_and was examined and left UNCHANGED: its per-timestep work is trivial bitwise AND/XNOR on already-int8 (stype) spikes where the .type(torch.int8) casts are no-ops, and cost is dominated by encoder/decoder; no safe speedup. Note: mul_csg is a dependency of module/rnn.py (an rnn cell uses it); the edit is bit-exact so downstream behavior is unchanged, to be confirmed by the central sweep gate. |
| 2026-06-13 | operation.relu_cnt | src/napl/operation/relu.py | 8fd6863d7e6d6bd1319af3434e586f7dfff28093 | yes | (napl-port-unarysim Improve) |  |  | relu_cnt: dropped the redundant below_half.type(torch.int8) cast in the per-timestep forward. torch.int8 \| bool already yields int8 in PyTorch, so input.type(torch.int8) \| below_half produces the identical int8 output as before, removing one tensor cast/allocation per timestep (256/forward). Bit-exact (verified max abs error 0.0 vs torch.nn.ReLU on both CPU and MPS). The macro-benchmark cannot resolve the win: total runtime is dominated by the encoder/decoder/lt/acc-update ops, and the per-run timing noise (CPU 7.9-9.4ms, MPS 20.6-28.7ms across repeated runs) is far larger than the one-cast saving, so measured CPU/MPS times are flat-within-noise (~1.0x). relu_sat (two add_any saturating adders, the algorithm's structure, not overhead), relu_hub (single fused torch.nn.functional.hardtanh, single-shot), and the shared add_any dependency were already optimized in prior passes (in-place add_/clamp_/addcmul_, pre-reduced dim=None path, fused acc update); no further behavior-preserving speedup found for them. relu_hub has no committed test (exercised indirectly) and was left unchanged. |
| 2026-06-13 | operation.relu_hub | src/napl/operation/relu.py | 8fd6863d7e6d6bd1319af3434e586f7dfff28093 | yes | (napl-port-unarysim Improve) |  |  | relu_cnt: dropped the redundant below_half.type(torch.int8) cast in the per-timestep forward. torch.int8 \| bool already yields int8 in PyTorch, so input.type(torch.int8) \| below_half produces the identical int8 output as before, removing one tensor cast/allocation per timestep (256/forward). Bit-exact (verified max abs error 0.0 vs torch.nn.ReLU on both CPU and MPS). The macro-benchmark cannot resolve the win: total runtime is dominated by the encoder/decoder/lt/acc-update ops, and the per-run timing noise (CPU 7.9-9.4ms, MPS 20.6-28.7ms across repeated runs) is far larger than the one-cast saving, so measured CPU/MPS times are flat-within-noise (~1.0x). relu_sat (two add_any saturating adders, the algorithm's structure, not overhead), relu_hub (single fused torch.nn.functional.hardtanh, single-shot), and the shared add_any dependency were already optimized in prior passes (in-place add_/clamp_/addcmul_, pre-reduced dim=None path, fused acc update); no further behavior-preserving speedup found for them. relu_hub has no committed test (exercised indirectly) and was left unchanged. |
| 2026-06-13 | operation.relu_sat | src/napl/operation/relu.py | 8fd6863d7e6d6bd1319af3434e586f7dfff28093 | yes | (napl-port-unarysim Improve) |  |  | relu_cnt: dropped the redundant below_half.type(torch.int8) cast in the per-timestep forward. torch.int8 \| bool already yields int8 in PyTorch, so input.type(torch.int8) \| below_half produces the identical int8 output as before, removing one tensor cast/allocation per timestep (256/forward). Bit-exact (verified max abs error 0.0 vs torch.nn.ReLU on both CPU and MPS). The macro-benchmark cannot resolve the win: total runtime is dominated by the encoder/decoder/lt/acc-update ops, and the per-run timing noise (CPU 7.9-9.4ms, MPS 20.6-28.7ms across repeated runs) is far larger than the one-cast saving, so measured CPU/MPS times are flat-within-noise (~1.0x). relu_sat (two add_any saturating adders, the algorithm's structure, not overhead), relu_hub (single fused torch.nn.functional.hardtanh, single-shot), and the shared add_any dependency were already optimized in prior passes (in-place add_/clamp_/addcmul_, pre-reduced dim=None path, fused acc update); no further behavior-preserving speedup found for them. relu_hub has no committed test (exercised indirectly) and was left unchanged. |
| 2026-06-13 | operation.round_fxp | src/napl/operation/round.py | 3fccf0092b103aeb52dfd3bae4e7a2ef8eab5fff | no | (napl-port-unarysim Improve) |  |  | No change. round_fxp is a single-shot binary-domain op delegating to round_ste, whose hot path is already a minimal vectorized chain: pow2_lshift (1 alloc) -> in-place round_().clamp_() (already fused per existing comment) -> pow2_rshift (1 alloc), with the two .to() casts being no-ops when input is float32. Runtime is dominated by the unavoidable elementwise multiply/round/clamp/divide passes; no per-timestep loop, no redundant clone/cast/alloc to remove. The only candidate micro-opts (hoist the 2.0**fracwidth scalar; power-of-two reciprocal multiply instead of divide) live inside the shared pow2_lshift/pow2_rshift utility shims, which must not be touched (surgical/shared-primitive discipline); inlining them for a sub-microsecond gain is not a safe or worthwhile speedup. Baseline cpu 0.438 ms / mps 0.361 ms for a 1M-element tensor; speedup 1.00x both devices. |
| 2026-06-13 | operation.shiftreg | src/napl/operation/shiftreg.py | 87b81cfd751e2c90ab51cbba94379cbbd6e3dfdb | yes | (napl-port-unarysim Improve) |  |  | Replaced the per-timestep stacked-tensor circular buffer with a collections.deque FIFO of row-tensor references, built lazily from self.reg on the first forward. Per-timestep work drops from {index a view, .clone() (alloc+copy), indexed in-place copy of input into the slot, head modulo arithmetic} to a pure popleft (read oldest) + append (store the fresh input reference). This removes the per-step clone allocation and the indexed copy entirely; FIFO semantics are identical to the roll/circular-buffer (oldest-out, newest-in, depth-cycle latency, i%2 init pattern). Safe because input is a fresh tensor each call (no aliasing) and the popped reference is never mutated. self.reg Parameter is preserved for HW/init/inspection (the test prints reg[idx] and hw_params(pp_delay=depth) is unchanged), and reset() rebuilds the FIFO via re-init. Bit-exact vs an explicit-FIFO reference on cpu and mps for depths 1/2/3/5 plus reset re-run; downstream dependent sqrt_emit (the only consumer) still passes. Behavior-preserving, no dtype/shift/import-cycle conventions touched. Baseline->optimized (256 timesteps, depth=2, shape (10,)): cpu 1.34->~0.45 ms (~3x), mps 3.02->~0.60 ms (~5x). |
| 2026-06-13 | operation.sigmoid_hard | src/napl/operation/sigmoid.py | 936b11e7a7f6c32249f85577b4a964a35a3af107 | no | (napl-port-unarysim Improve) |  |  | No safe speedup found; nothing changed this session. Both classes are thin wrappers with no optimizable hot path of their own. sigmoid_hub.forward is a single fused torch primitive hardsigmoid(input * scale) (single-shot, trainable binary-domain via STE, so in-place is forbidden) -- already optimal. sigmoid_hard.forward is self.tick() + self.scaled_add(input + 1, dim=None, entry=2); the per-timestep cost lives entirely in the shared add_any primitive (operation/add.py), which is already heavily optimized (in-place fused accumulate/clamp + addcmul_, no extra per-timestep allocations). The only marginal extra in sigmoid_hard is the input + 1 elementwise op, and folding that into add_any's offset would require editing the shared add_any contract (out of scope, risky cross-file behavior change); the existing input + 1/entry=2 form is itself a prior optimization (avoids stacking an all-ones tensor). Per skill guidance, changed nothing. Note: git diff shows the file already differs from HEAD due to that pre-existing working-tree optimization, but no edits were made in this session. |
| 2026-06-13 | operation.sigmoid_hub | src/napl/operation/sigmoid.py | 936b11e7a7f6c32249f85577b4a964a35a3af107 | no | (napl-port-unarysim Improve) |  |  | No safe speedup found; nothing changed this session. Both classes are thin wrappers with no optimizable hot path of their own. sigmoid_hub.forward is a single fused torch primitive hardsigmoid(input * scale) (single-shot, trainable binary-domain via STE, so in-place is forbidden) -- already optimal. sigmoid_hard.forward is self.tick() + self.scaled_add(input + 1, dim=None, entry=2); the per-timestep cost lives entirely in the shared add_any primitive (operation/add.py), which is already heavily optimized (in-place fused accumulate/clamp + addcmul_, no extra per-timestep allocations). The only marginal extra in sigmoid_hard is the input + 1 elementwise op, and folding that into add_any's offset would require editing the shared add_any contract (out of scope, risky cross-file behavior change); the existing input + 1/entry=2 form is itself a prior optimization (avoids stacking an all-ones tensor). Per skill guidance, changed nothing. Note: git diff shows the file already differs from HEAD due to that pre-existing working-tree optimization, but no edits were made in this session. |
| 2026-06-13 | operation.sign_abs | src/napl/operation/sign_abs.py | e4315dc88c569bbbc5a9fb01a975263fcda8492b | no | (napl-port-unarysim Improve) |  |  | No safe behavior-preserving speedup applied; file unchanged. The forward() is already fully vectorized (one elementwise add/clamp/lt/xor reduction per timestep over 10000 elements). Benchmarked a candidate that removes the redundant final .type(self.stype) casts by computing sign/abs directly in int8: it was bit-exact (torch.equal every timestep) but only ~1.06x CPU / ~1.03x MPS, since the cost is dominated by the per-timestep elementwise reduction, not the casts. That candidate also hardcodes int8 output, dropping the original's self.stype output-dtype contract (legal stype includes float/bfloat16, where the original casts the int8 xor result up to the float spike type), which would change the dtype contract for a marginal gain. Per the hard constraint that outputs must not change, nothing was edited. Baseline timing for reference: CPU 11.53 ms, MPS 31.56 ms per 256-timestep workload. |
| 2026-06-13 | operation.sqrt_emit | src/napl/operation/sqrt.py | f3fb4dc9d32301597133d185d7c3446979ac3f27 | yes | (napl-port-unarysim Improve) |  |  | Optimized sqrt_emit.forward only. Replaced the per-timestep torch.stack([input.type(int8), self.emit_out], dim=0) followed by self.nsadd(in_stack, dim=0) (a 2-element reduction) with a direct int8 elementwise sum in_sum = input.type(int8) + self.emit_out fed pre-reduced via self.nsadd(in_sum, dim=None). This removes the [2, N] stack allocation every timestep. Both operands are {0,1} so the int8 sum (max 2) cannot overflow, and nsadd (add_any, unipolar, dim=None path) re-casts the partial to ntype just as torch.sum(stack, dtype=ntype) did, so the result is bit-identical; unipolar needs no entry. Verified bit-exact (torch.equal) against the original stack form on CPU and MPS over the test's 256-timestep / 10000-element workload. sqrt_tracejkff and sqrt_traceiscb were NOT changed: their forwards are a small spike-insert formula plus already-optimized dependency calls (jkff/bi2uni/div_cordiv), the .type() casts are intentional float-stype safety, and the only available rewrites (e.g. &/+ -> \|) are risky for marginal gain. Per-device medians (baseline -> optimized): sqrt_emit cpu ~12.5 -> ~10.3-11.7 ms (~1.1-1.2x), mps ~46 -> ~38-45 ms (~1.05-1.2x). The other two classes' fluctuations are measurement noise (unchanged code). Skill Step 6 recorder and full downstream sweep intentionally skipped per batch overrides; sqrt_emit is not a shared primitive imported by other ops. |
| 2026-06-13 | operation.sqrt_traceiscb | src/napl/operation/sqrt.py | f3fb4dc9d32301597133d185d7c3446979ac3f27 | yes | (napl-port-unarysim Improve) |  |  | Optimized sqrt_emit.forward only. Replaced the per-timestep torch.stack([input.type(int8), self.emit_out], dim=0) followed by self.nsadd(in_stack, dim=0) (a 2-element reduction) with a direct int8 elementwise sum in_sum = input.type(int8) + self.emit_out fed pre-reduced via self.nsadd(in_sum, dim=None). This removes the [2, N] stack allocation every timestep. Both operands are {0,1} so the int8 sum (max 2) cannot overflow, and nsadd (add_any, unipolar, dim=None path) re-casts the partial to ntype just as torch.sum(stack, dtype=ntype) did, so the result is bit-identical; unipolar needs no entry. Verified bit-exact (torch.equal) against the original stack form on CPU and MPS over the test's 256-timestep / 10000-element workload. sqrt_tracejkff and sqrt_traceiscb were NOT changed: their forwards are a small spike-insert formula plus already-optimized dependency calls (jkff/bi2uni/div_cordiv), the .type() casts are intentional float-stype safety, and the only available rewrites (e.g. &/+ -> \|) are risky for marginal gain. Per-device medians (baseline -> optimized): sqrt_emit cpu ~12.5 -> ~10.3-11.7 ms (~1.1-1.2x), mps ~46 -> ~38-45 ms (~1.05-1.2x). The other two classes' fluctuations are measurement noise (unchanged code). Skill Step 6 recorder and full downstream sweep intentionally skipped per batch overrides; sqrt_emit is not a shared primitive imported by other ops. |
| 2026-06-13 | operation.sqrt_tracejkff | src/napl/operation/sqrt.py | f3fb4dc9d32301597133d185d7c3446979ac3f27 | yes | (napl-port-unarysim Improve) |  |  | Optimized sqrt_emit.forward only. Replaced the per-timestep torch.stack([input.type(int8), self.emit_out], dim=0) followed by self.nsadd(in_stack, dim=0) (a 2-element reduction) with a direct int8 elementwise sum in_sum = input.type(int8) + self.emit_out fed pre-reduced via self.nsadd(in_sum, dim=None). This removes the [2, N] stack allocation every timestep. Both operands are {0,1} so the int8 sum (max 2) cannot overflow, and nsadd (add_any, unipolar, dim=None path) re-casts the partial to ntype just as torch.sum(stack, dtype=ntype) did, so the result is bit-identical; unipolar needs no entry. Verified bit-exact (torch.equal) against the original stack form on CPU and MPS over the test's 256-timestep / 10000-element workload. sqrt_tracejkff and sqrt_traceiscb were NOT changed: their forwards are a small spike-insert formula plus already-optimized dependency calls (jkff/bi2uni/div_cordiv), the .type() casts are intentional float-stype safety, and the only available rewrites (e.g. &/+ -> \|) are risky for marginal gain. Per-device medians (baseline -> optimized): sqrt_emit cpu ~12.5 -> ~10.3-11.7 ms (~1.1-1.2x), mps ~46 -> ~38-45 ms (~1.05-1.2x). The other two classes' fluctuations are measurement noise (unchanged code). Skill Step 6 recorder and full downstream sweep intentionally skipped per batch overrides; sqrt_emit is not a shared primitive imported by other ops. |
| 2026-06-13 | operation.square_dff | src/napl/operation/square.py | 48af0a2ca6bd149493da26c48a244fe2dd508f29 | yes | (napl-port-unarysim Improve) |  |  | square_dff.forward: cached self._spike_is_int8 = (self.stype == torch.int8) in __init__, and on that common default-stype fast path skip the three redundant per-timestep casts (.type(torch.int8) on both operands plus the final .type(self.stype)) since int8 spike operands and the 1-(a^b) result are already int8. The float/bfloat16 stype branch keeps the casts (bitwise ops need an integer operand). Also made the bipolar XNOR precedence explicit: 1-(a^b) instead of the HEAD form 1 - input ^ input_d which parses as (1-a)^b; verified bit-identical over the full 0/1 truth table (true XNOR), so behavior is preserved and the parenthesized form is the faster MPS variant. Bit-exact equivalence confirmed for unipolar and bipolar across 20 timesteps and for the forced float-stype path. Isolated 10000-element x 256-timestep timing (median, multiple runs): CPU ~2.24 -> ~2.0-2.19 ms (~1.05-1.12x), MPS ~3.5-4.8 -> ~3.2-4.1 ms (~1.05-1.2x); modest since the workload is also bound by dff/encoder/decoder. Note: HEAD lacked self.delay; left it absent to keep the diff surgical to the optimization. |
| 2026-06-13 | operation.sync_skewed | src/napl/operation/sync.py | 2f5073d2bafe458a38d786fb7e1b6a939c0c9fdd | no | (napl-port-unarysim Improve) |  |  | No new edits this run. The file already carried behavior-preserving optimizations (uncommitted vs HEAD) for class sync_skewed: (1) the per-timestep select term cnt_not_min*(1-input_1) - cnt_not_max*input_1 rewritten as cnt_not_min - (cnt_not_min + cnt_not_max).mul(input_1), reducing int8 elementwise ops (exact because input_1 is a {0,1} spike); (2) dropped redundant .type(self.ntype) casts in the in-place cnt accumulate (the int8 operand safely promotes into the float32 cnt.data destination, so the cast was wasted work). I verified these are a real, behavior-preserving speedup over HEAD: CPU 17.97->15.39 ms (1.17x), MPS 71.73->55.14 ms (1.30x), same fixed-seed inputs (10000-elem streams, 256 timesteps). I evaluated further micro-fusions of the hot path but found no additional safe win worth the risk to the clean stype/ntype dtype contract, so per coding-discipline and the skill's guidance I changed nothing further. |
| 2026-06-13 | operation.tanh_hard | src/napl/operation/tanh.py | de01f7c39bff285aa8dc028058ca0155639a658f | no | (napl-port-unarysim Improve) |  |  | No changes made; both classes are already minimal/optimal so there is no safe behavior-preserving speedup (reported ~1.0x). tanh_hard.forward() is a pure passthrough that only calls self.tick() and returns the input unchanged: no compute, allocation, dtype cast, or per-timestep Python loop to vectorize (its measured time is dominated entirely by the encoder/decoder in the 256-step harness, not the op). tanh_hub.forward() is a single torch.nn.functional.hardtanh(input, -1, 1) call, which already lowers to the same fused C++ clamp kernel as any hand-rolled equivalent (torch.clamp), so nothing can be removed without altering behavior. Baseline timing (unchanged): cpu tanh_hard 4.70 ms / tanh_hub 0.0020 ms; mps tanh_hard 14.39 ms / tanh_hub 0.162 ms. Correctness re-verified on CPU and MPS: tanh_hub bit-exact vs F.hardtanh, tanh_hard round-trip max_err=0.0000. |
| 2026-06-13 | operation.tanh_hub | src/napl/operation/tanh.py | de01f7c39bff285aa8dc028058ca0155639a658f | no | (napl-port-unarysim Improve) |  |  | No changes made; both classes are already minimal/optimal so there is no safe behavior-preserving speedup (reported ~1.0x). tanh_hard.forward() is a pure passthrough that only calls self.tick() and returns the input unchanged: no compute, allocation, dtype cast, or per-timestep Python loop to vectorize (its measured time is dominated entirely by the encoder/decoder in the 256-step harness, not the op). tanh_hub.forward() is a single torch.nn.functional.hardtanh(input, -1, 1) call, which already lowers to the same fused C++ clamp kernel as any hand-rolled equivalent (torch.clamp), so nothing can be removed without altering behavior. Baseline timing (unchanged): cpu tanh_hard 4.70 ms / tanh_hub 0.0020 ms; mps tanh_hard 14.39 ms / tanh_hub 0.162 ms. Correctness re-verified on CPU and MPS: tanh_hub bit-exact vs F.hardtanh, tanh_hard round-trip max_err=0.0000. |
| 2026-06-13 | operation.uni2bi | src/napl/operation/uni2bi.py | 010c814cc21f4140d3fead588add411ee0c99a9c | yes | (napl-port-unarysim Improve) |  |  | In uni2bi.forward, the carry-out spike was previously cast to ntype (for the in-place acc.sub_) and then cast again to stype for the return (two casts/timestep). Since stype (int8) is a safe widening into the float acc for in-place sub_(..., alpha=2), I now cast torch.ge(acc, 2) directly to stype once and reuse that single tensor for both the accumulator update and the return. This eliminates one elementwise cast (one kernel dispatch) per timestep, which most benefits MPS where per-op launch overhead dominates. Bit-exact verified against the pre-edit forward on both cpu and mps. Measured medians: cpu ~7.1->~5.2 ms (~1.37x), mps ~34.0->~22.3 ms (~1.52x). No dtype-promotion or scalar-broadcast hazard: the [1]->[N] first-call broadcast path (out-of-place add/clamp) is untouched, and the in-place sub_ destination (float acc) safely accepts the int8 operand. |
