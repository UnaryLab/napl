# napl port-from-UnarySim (gen-sim) log

Records produced by the `napl-gen-sim` skill - one row per UnarySim class ported into napl. Each row
attests that a UnarySim implementation was reimplemented in napl's conventions (napl_base subclass,
config/key_list validation, stype/ntype dtypes, the streaming `@napl_sim_timesteps` paradigm or the
single-shot binary-domain form, the hw_params contract, napl naming), given a `test_<name>.py`, and
validated faithful against the UnarySim original. **Status** is `ported` (new napl class created and
validated), `skipped` (no sound napl mapping, e.g. a backward-only autograd helper), or `failed`.
**Validated** records the napl-vs-UnarySim agreement (bit-exact or within the SC/quant bound).

| Date | napl class | UnarySim source | Status | Test | Validated | Notes |
|------|------------|-----------------|--------|------|-----------|-------|
| 2026-07-11 | metric.stability_builder | NSbuilder | ported | tests/metric/test_stability_builder.py | bit-exact vs UnarySim NSbuilder: 0 mismatched bits across 20 configs (unipolar+bipolar x normstability {0,0.3,0.5,0.8,1.0} x threshold {0.05,0.1}, 64 values x 256 timesteps each); own test passes on CPU+MPS with identical numbers (decode err <= threshold, measured stability tracks requested normstability, reset exactness) | Faithful torch reimplementation of NSbuilder (incl. its search_best_stab_parallel_numpy helper, ported as torch float64 module-level search_best_stab). napl conventions: napl_base config dict (polarity/threshold/normstability/timestep/generator[/dim]), stype/ntype, tick()/reset(), reuses gen_num_seq from module/encoder for the Sobol sequence (num_seq*L floored matches UnarySim RNG bit-exactly). Build-time search runs on CPU (float64+gcd); module streams on any device after .to(). Did NOT edit metric/__init__.py or mapping.md, and did NOT write reports/napl-gen-sim-report.md, per workflow overrides; test imports the class directly from napl.sim.metric.stability_builder. |
| 2026-07-11 | metric.stability_norm | NormStability | ported | tests/metric/test_stability_norm.py | bit-exact (torch.equal) vs UnarySim NormStability on identical streams: unipolar+bipolar, T=200/256, threshold=0.05/0.1, 500 elements | Wraps napl metric.stability and adds the max-stability search (torch port of UnarySim search_best_stab_parallel_numpy, exposed as search_max_stab; float64 arithmetic and identical iteration count to reproduce numpy's promotion and the reference's overwrite-style best-update, so results are bit-exact). Search runs once on CPU in report_stab_norm (torch.gcd), result moved back to the source device. Test covers CPU+MPS with identical inputs, [0,1] range, cross-device agreement, known-answer constant stream = 1.0, reset, and per-device timing. Not wired into metric/__init__.py per workflow instruction. |
| 2026-07-11 | module.avgpool2d | FSUAvgPool2d | ported | tests/module/test_avgpool2d.py | bit-exact vs FSUAvgPool2d over 256 timesteps (unipolar + bipolar, identical spike streams, CPU); own test passes on CPU+MPS with RMSE ~0.002-0.004 vs F.avg_pool2d (bound 0.25) | Streaming scaled-add pooling: per timestep acc += AvgPool2d(spikes), emit spike where acc>=1, subtract. Uses napl scalar-accumulator broadcast idiom, so UnarySim's input_shape constructor arg is dropped; stride/padding/ceil_mode delegate to nn.AvgPool2d (strictly more general than UnarySim, whose explicit-stride path is an unimplemented `pass`). Polarity-agnostic math (affine rate->value mapping); polarity kept in config per napl convention. No hw_params (module, not gate-level op). Not wired into module/__init__.py and mapping.md not updated, per scope. |
| 2026-06-13 | module.conv_pc | FSUConv2dPC | ported | tests/module/test_conv_pc.py | Faithful vs FSUConv2dPC on identical input spike streams (CPU+MPS), both polarities x bias on/off x pad 0/1. bit-exact=False as expected (each side encodes the weight/bias stream with its own RNG, so cannot match bit-for-bit). Both reproduce conv2d(x,W)+b within the SC bound: napl-vs-conv2d rmse 0.005-0.024, unarysim-vs-conv2d rmse 0.009-0.021, napl-vs-unarysim rmse 0.011-0.033, all well under the SC bound 0.133 (T=512). Stochastic-port regime: within-bound agreement, not bit-exact. | Streaming paradigm. Parallel-counter variant of conv: per-timestep it encodes weights (and bias) on a distinct sobol dim, unfolds the input to patches, returns the AND-count (unipolar) / XNOR-count (bipolar) per output element via matmul + fold, WITHOUT conv's add_any scaled accumulator. The conv counterpart of the existing linear_pc and mirrors its bias placement (input-1 path only) and bipolar XNOR-count form (matmul(x,w)+matmul(1-x,1-w)). Reuses conv's decorrelated rate-0.5 pad stream for bipolar zero-padding. Per-timestep count lies in [0, entry] with entry=in*kh*kw+has_bias; accumulate/T then 2*mean-entry (bipolar) or mean (unipolar) recovers the conv. Lazy encoder import (module<->operation cycle). groups=1, zero padding only. Test passes on CPU+MPS (pytest 1 passed). NOT wired into module/__init__.py and mapping.md NOT edited per task override (workflow does the wiring); Step 6 recorder NOT run per override. Test imports the class directly from napl.sim.module.conv_pc. |
| 2026-07-11 | module.conv_ugemm | FSUConv2duGEMM | ported | tests/module/test_conv_ugemm.py | bit-exact vs FSUConv2duGEMM (scaled=True), per-timestep torch.equal over T=256, all 8 regimes (uni/bipolar x bias on/off x pad 0/1), CPU+MPS, cpu==mps; decoded rmse <= 0.0049 vs F.conv2d/entry (SC bound 0.062) | Streaming conv2d with uGEMM conditional spike generation: weight bitstream index advances by the input spike (mul_csg idiom; bipolar adds the input-0 inverse path), bias free-running on the shared dim-1 RNG, scaled output via add_any (scale defaults to entry=in*kh*kw+has_bias). Bipolar zero-pad alternates 0/1 (UnarySim even_cycle_flag), unlike conv's decorrelated pad encoder. Non-scaled UnarySim mode not ported (napl conv family is scaled-only). Prob-vs-num_seq compare proven equivalent to UnarySim SourceGen/BSGen integer compare for 2^-8-quantized operands. Own test passes CPU+MPS (correctness + perf, ~1.1x vs conv on MPS). Per workflow overrides: no report rows written; module/__init__.py and mapping.md left for the serialized wiring step. |
| 2026-07-11 | module.gru_hardnuapt | HardGRUCellNUAPT | ported | tests/module/test_gru_hardnuapt.py | bit-exact vs UnarySim HardGRUCellNUAPT (torch.equal, hard=True and hard=False, identical inputs and copied weights, incl. hx=None path) | Single-shot binary-domain GRU cell with hard activations (sigmoid_hub scale-3 hard sigmoid, tanh_hub hard tanh; hard=False falls back to Sigmoid/Tanh and matches nn.GRUCell exactly). napl_base subclass, PyTorch GRUCell 3-chunk weight layout, truncated_normal init (stdv=1/sqrt(hidden)). Test covers CPU+MPS, correctness (independent reference + nn.GRUCell known-answer), gradient flow, and timing vs nn.GRUCell (~1.2x slower, hard activations cost). Not wired into module/__init__.py per scope; recorder skipped per workflow override. |
| 2026-07-11 | module.linear_ugemm | FSULinearuGEMM | ported | tests/module/test_linear_ugemm.py | bit-exact (torch.equal, max\|diff\|=0 per timestep and on total spike counts) vs UnarySim FSULinearuGEMM(scaled=True) on identical spike streams, all 4 regimes (unipolar/bipolar x bias/no-bias), CPU + MPS; decoded value within SC bound of (Wx+b)/entry (rmse 0.0009-0.0041, T=256) | Streaming kernel: uGEMM conditional spike generation (mul_csg-style input-driven RNG indices for weight bits; bipolar adds complemented input-0 path; bias bit advances unconditionally) feeding the existing add_any scaled accumulator (scale=entry default, exactly UnarySim's scaled path). Only scaled=True is ported; UnarySim's scaled=False output-comparator variant is not (napl add_any has no equivalent; same limitation as linear) - noted in the docstring. Own test passes on CPU+MPS incl. known-answer corner and perf vs linear (~1.2x faster on MPS, identical inputs, synced timing). Per task overrides: no report rows written, module/__init__.py and mapping.md untouched. Bit-exactness requires width-8-quantized operands (probs integral multiples of 1/256), as with the other CSG validations. |
| 2026-07-11 | module.linear_gaines1 | GainesLinear1 | ported | tests/module/test_linear_gaines1.py | agree within SC bound: worst napl-vs-UnarySim decoded RMSE 0.0644 (~1.03x SC bound 0.0625 at T=256, tolerance 3x), identical input spike stream (UnarySim SourceGen/BSGen Sobol dim 2) fed to both across all 8 configs (uni/bi x scaled/non-scaled x bias/no-bias), CPU and MPS identical. Not bit-exact by design: internal weight/scale RNGs differ (napl scalar-sobol-per-encoder + gt quantization vs UnarySim per-column RNGMulti + round/floor), same precedent as div_cordiv row. Non-scaled bipolar compared on \|s\|>1 (sign-detection steady state); for \|s\|<1 the bistable counter is RNG-trajectory dependent in BOTH implementations (UnarySim itself is 0.82 RMSE off analytic there). | Streaming gMUL+gADD FC layer next to linear: weight/bias encoders on distinct sobol dims, XNOR/AND parallel count via matmul, then Gaines adder: scaled mode compares the count >= random level in [0, 2**round(log2(entry))) (own sobol dim); non-scaled unipolar emits count>0 (OR), non-scaled bipolar drives a depth-bit saturating up/down counter (broadcasting scalar init 2**(depth-1)). Config keys polarity/timestep/generator (+dim, scaled, depth). Own test passes CPU+MPS (scaled uni/bi x bias RMSE 0.018-0.097, known-answer all-ones corner, perf: 1.25x faster than linear on MPS on identical spikes). Harness note: upstream GainesLinear1 non-scaled bipolar crashes on modern torch (float input_cnt sub_'d into long tensor); validation harness losslessly casts ref.input_cnt to int. mapping.md and module/__init__.py deliberately untouched per scope; Step 6 recorder skipped per override. |
| 2026-07-11 | module.linear_gaines2 | GainesLinear2 | ported | tests/module/test_linear_gaines2.py | bit-exact vs GainesLinear2 on identical input bitstreams (CPU, T=256, in=16 out=8): unipolar exact in all 4 scaled/bias configs; bipolar exact once weights are quantized to UnarySim's rounded SourceGen levels (raw bipolar diff is only that source rounding, decoded \|diff\| <= 0.039 < SC bound 0.0625). Own test passes on CPU+MPS, all polarity/scaled/bias combos within SC bound. | Streaming gMUL + uADD: per-weight-column Sobol dims (col j -> dim j+1, bias -> dim in+1, matching UnarySim RNGMulti/RNG), XNOR/AND count via the matmul identity (no (out,in) product materialized). scaled=True reuses operation.add_any with scale=entry (zero offset, identical emit rule); scaled=False implements the non-scaled Gaines output comparator (offset (in-1)/2 + 0.5*bias for bipolar) with napl's broadcast-on-first-forward accumulator idiom. Config keys: polarity, timestep, generator (sobol family required), scaled (default True), width (default 12, asserted >= fan-in). Test: correctness sweep on every device (cpu+mps here), known-answer all-ones corner, perf vs linear (1.27x faster on MPS). Did not touch module/__init__.py or mapping.md per scope; no report row written (workflow records centrally). |
| 2026-07-11 | module.linear_gaines3 | GainesLinear3 | ported | tests/module/test_linear_gaines3.py | bit-exact (torch.equal, max\|diff\|=0) vs GainesLinear3 in all 8 regimes (unipolar/bipolar x scaled/non-scaled x bias/no-bias) on CPU+MPS; identical input spike streams (UnarySim BSGen sobol dim 2, also verified identical to napl encoder); T=256 in=32 out=8 bitwidth=8; runtimes CPU 6.7 ms / MPS 47.5 ms | uMUL + gADD streaming Gaines FC layer. Reuses operation.mul_csg for the shared-RNG weight CSG and module.encoder (dim 1) for the bias stream; gADD scaled = ge(count, sobol dim-6 scale seq of width round(log2(entry))), non-scaled = OR (unipolar) / saturating depth-bit counter (bipolar). Config keys: polarity/timestep/generator + optional scaled (True), depth (8), scale_dim (6). Test passes CPU+MPS (script and pytest) incl. known-answer corners, cross-device bit-exact counts, and perf vs linear (CPU 0.95x, MPS 1.63x). UnarySim non-scaled path needed a harness-side int() patch of its input_cnt (upstream in-place float-on-long issue on modern torch), not a divergence. Shared files untouched (module/__init__.py, mapping.md, reports) per workflow overrides. |
| 2026-07-11 | module.linear_gaines4 | GainesLinear4 | ported | tests/module/test_linear_gaines4.py | bit-exact vs GainesLinear4 (0/16384 bit mismatches per regime; decoded max\|diff\|=0) across CPU+MPS x {unipolar,bipolar} x {scaled,non-scaled} x {bias,no-bias}, with identical input spike streams and napl's LFSR sequences transplanted into UnarySim's RNG parameters (weights on width-8 grid so SourceGen rounding is exact) | Streaming Gaines linear (gMUL+gADD, LFSR flavor). Per-column decorrelated weight number sequences (RNGMulti/BSGenMulti analog; a single shared encoder sequence makes weight bits comonotone and biases the count), XNOR count via matmul + algebraic input-0-path identity, bias spike on forward kernel only, Gaines scaled add vs 2**round(log2(entry))-long LFSR threshold, non-scaled unipolar OR / bipolar saturating depth-bit counter (scalar broadcast-up accumulator idiom). Config keys: polarity, timestep, generator (lfsr default), dim, seed, scaled, depth. Known upstream-faithful quirks documented: scaled output is E[count]/(2**k-1) with an O(1/entry) systematic offset (thresholds {1..L-1}, >=, duplicated table state); sobol dim-1 input coding phase-locks with the L-periodic threshold table, so tests use an lfsr input codec. Perf: gaines 267ms vs linear 381ms (MPS, T=2048). Did NOT touch module/__init__.py, mapping.md, or reports/ (per scope/override). |
| 2026-07-11 | module.mgu_hardnua | HardMGUCellNUA | ported | tests/module/test_mgu_hardnua.py | bit-exact vs UnarySim HardMGUCellNUA (torch.equal) on identical inputs/weights, CPU + MPS; also matches an independent analytic NUA-MGU reference within 1e-6 | Single-shot binary-domain cell; = mgu_hard minus the hard-tanh clamps on the forget-gate linear and on hy (the NUA difference). Reuses _init_mgu_params from napl.sim.module.rnn and sigmoid_hub/tanh_hub (lazy import) from napl.sim.operation. Test covers correctness + gradients + perf per device (napl ~1.8x UnarySim on CPU, ~1.0x on MPS). Did not touch module/__init__.py, mapping.md, or the gen-sim report per workflow overrides. |
| 2026-07-11 | module.mgu_hardpt | HardMGUCellPT | ported | tests/module/test_mgu_hardpt.py | bit-exact vs UnarySim HardMGUCellPT (torch.equal, hard=True and hard=False, identical weights/inputs, incl. hx=None path) | Single-shot binary-domain MGU cell, PT (RNNCellBase num_chunks=2) parameter layout: weight_ih/weight_hh (2*hidden rows = [forget; new]) + bias_ih/bias_hh, truncated_normal init stdv=1/sqrt(hidden). Hard path uses lazily-imported sigmoid_hub (Hardsigmoid(x*3) == ScaleHardsigmoid) and tanh_hub; explicit hardtanh clamps keep intermediates in unary range. Test covers CPU+MPS (CUDA when available): exact match vs independent functional reference, hx=None, unary-range bound, finite STE gradients, and per-device timing vs the functional reference. Not wired into module/__init__.py per workflow (import directly from napl.sim.module.mgu_hardpt). |
| 2026-07-11 | operation.add_gaines | GainesAdd | ported | tests/operation/test_add_gaines.py | bit-exact vs GainesAdd per timestep and post-decode (max diff 0) on CPU+MPS for unipolar/bipolar scaled MUX and unipolar non-scaled OR, identical spikes via mirror encoders through the committed test wiring | Streaming combinational op (hw_params pp_delay=0). config: polarity, scaled (+entry/generator/dim when scaled); scaled=MUX via Sobol select seq (python-list idx, no per-timestep device sync), non-scaled=OR gate (unipolar only, bipolar rejected at init like UnarySim). Test passes CPU+MPS: scaled RMSE 0.076/0.154 (uni/bi) and OR RMSE 0.011 vs bound 0.1875, known-answer MUX/OR corners, perf vs add_any baseline (x108 CPU, x33 MPS). Per scope, did NOT write reports/napl-gen-sim-report.md, reports/napl-validate-unarysim-report.md, mapping.md, or operation/__init__.py; workflow records centrally and wires the package import. |
| 2026-07-11 | operation.add_ugemm | FSUAdduGEMM | ported | tests/operation/test_add_ugemm.py | bit-exact vs FSUAdduGEMM (torch.equal, max\|diff\|=0) on identical spike streams, all 4 regimes (unipolar/bipolar x scaled/non-scaled), CPU + MPS; test-wiring decoded values also match exactly | Streaming per-timestep op (napl_base, tick/reset, hw_params(pp_delay=0), scalar accumulator broadcast idiom). Config keys: polarity, scaled. Scaled mode = carry-out per acc_bound spikes (output ~ mean over dim); non-scaled = offset-corrected count vs emitted-spike count (output ~ clipped sum). Test covers CPU+MPS, correctness (MAE < 2/sqrt(T), worst 0.013 at T=256) and per-device timing (cpu ~8 ms, mps ~30-90 ms for 2000x8, T=256). Per task overrides: no report rows written, operation/__init__.py and mapping.md untouched (later serialized step wires them). |
| 2026-07-11 | operation.div_gaines | GainesDiv | ported | tests/operation/test_div_gaines.py | bit-exact vs UnarySim GainesDiv (torch.equal per timestep, max\|diff\|=0) on CPU+MPS, unipolar+bipolar, N=10000, depth=5, T=256, identical spike streams (Sobol dims 1/2 inputs, dim 3 op RNG) | Streaming per-timestep kernel: saturating up/down counter (depth-bit, init 2^(depth-1)) compared against floor(sobol*2^depth) RNG thresholds; unipolar integrates dividend - output&divisor, bipolar uses the XNOR/delayed-divisor feedback from UnarySim. napl adaptations: single config dict with polarity/depth/generator/dim, scalar scnt broadcasts to input shape on first forward, RNG thresholds kept as a Python list to avoid per-timestep GPU sync, combined add/sub/clamp counter update (bit-exact equivalent to UnarySim's two-stage blend), hw_params(pp_delay=1) since the quotient spike is a function of the counter register. Own test passes on CPU+MPS both polarities (RMSE 0.055 unipolar / 0.115 bipolar at T=256, bound 0.2); napl wiring runtime 13.5 ms CPU / 43.6 ms MPS. Recorders and mapping.md deliberately untouched per workflow overrides; class awaits wiring into operation/__init__.py by the serialized step. |
| 2026-07-11 | operation.exp_n1 | expN1 | ported | tests/operation/test_exp_n1.py | bit-exact vs UnarySim expN1 on CPU and MPS (torch.equal on full output streams, identical input spikes, N=10000, T=256); decoded RMSE vs exp(-x) 0.0065 within SC bound 0.0625 | Streaming unipolar-only exp(-x) via truncated Maclaurin series (Parhi & Liu 2017, Fig.12). Constants 0.2/0.25/0.3333/0.5 quantized to width-bit sources (round) on Sobol dims dim..dim+3, matching UnarySim SourceGen bit-for-bit; whole periodic constant spike table precomputed in __init__. 4-tap input delay line uses napl's reference-holding buffer idiom; hw_params(pp_delay=0) (combinational input->output path, DFFs only decorrelate). Test runs CPU+MPS with RMSE bound 1.5/sqrt(T), known-answer corners exp(0)=1 and exp(-1), and per-device timing. Gotcha found: MPS bitwise & with a 0-dim int8 operand returns wrong values; module uses 1-element slices (c[0:1]) as workaround with comment. Did NOT edit operation/__init__.py or mapping.md, and did NOT run the Step 6 recorder, per workflow overrides. |
| 2026-07-11 | operation.exp_ng | expNG | ported | tests/operation/test_exp_ng.py | bit-exact vs UnarySim expNG on identical spike streams, 256 timesteps x 1000 elems, CPU + MPS (torch.equal per timestep) | FSM exp(-2*gain*x) (Brown & Card): bipolar rate-coded input (non-negative value), unipolar output; saturating up/down counter, config keys depth (required) and gain (default 1); napl scalar-accumulator broadcast idiom; hw_params(pp_delay=1) since output is combinational from the state register only (RTL step may refine). Test asserts mean abs error < 0.05 vs exp(-2x) (measured MAE 0.029, depth=5, 256 timesteps) on cpu+mps with identical inputs and GPU sync before timing. Per overrides: operation/__init__.py, mapping.md, and reports/ NOT touched. |
| 2026-07-11 | operation.mul_gaines | GainesMul | ported | tests/operation/test_mul_gaines.py | bit-exact vs UnarySim GainesMul on identical random spike inputs, both polarities, CPU and MPS; round-trip test passes within SC bound on cpu+mps | GainesMul is gate-identical to the existing mul_and (unipolar AND / bipolar XNOR); ported as a standalone combinational op (hw_params pp_delay=0) to mirror UnarySim's module boundary, noted in the class docstring. Test covers correctness (SC-bound round-trip, 4-entry truth-table known answers) and perf (vs mul_and baseline, ~parity: x1.02 cpu, x1.11 mps). Per overrides: operation/__init__.py, mapping.md, and reports/ untouched; test imports the class directly from napl.sim.operation.mul_gaines. |
| 2026-07-11 | operation.sqrt_gaines | GainesSqrt | ported | tests/operation/test_sqrt_gaines.py | bit-exact vs GainesSqrt on identical spike streams (torch.equal per timestep and decoded, max\|diff\|=0), CPU+MPS, unipolar+bipolar, N=1000 T=256; napl runtime 5.7 ms CPU / 23.9 ms MPS | Streaming per-timestep port of GainesSqrt (saturating up/down counter thresholded by a Sobol num_seq; dec feedback = out AND out_d unipolar / out XNOR out_d bipolar). napl conventions: napl_base config dict with keys polarity/width/generator(+dim); UnarySim's `depth` (counter bit width) maps to napl's `width` (relu_cnt convention); num_seq via gen_num_seq*2^width floored, matching UnarySim RNG integers exactly; RNG index kept as a Python list/int (no per-timestep GPU sync); scalar scnt/out_d broadcast to input shape on first forward; hw_params(pp_delay=0). Test covers cpu+mps, both polarities, RMSE bound 0.15 (measured ~0.09/0.10 at T=256, Gaines sqrt is biased near zero so looser than 1/sqrt(N)), plus timing and reset checks. Not wired into operation/__init__.py and mapping.md not updated per task scope; no report row written (workflow records centrally). |
| 2026-07-11 | operation.sync_skewed_int | SkewedSyncInt | ported | tests/operation/test_sync_skewed_int.py | bit-exact vs UnarySim SkewedSyncInt on CPU and MPS: per-timestep outputs and decoded values torch.equal (max\|diff\|=0.0), identical Sobol-encoded inputs (unipolar, N=10000, T=256, width/depth=4), both in_1<=in_2 and in_1>in_2 (integer aggregation) regimes; committed test wiring also bit-exact vs UnarySim decode | Streaming op, sibling of sync_skewed but config key 'width' default 4 (UnarySim depth=4); output_1 is an integer digit stream (0..2^width-1), not {0,1}, cast to stype (fits int8 for width<=7). Counter uses napl's out-of-place broadcast idiom (no is_first_call needed). hw_params(pp_delay=0). Test passes on CPU+MPS: SC conservation bound (cnt residual), exact pass-through of output_2, known-answer digit-2 aggregation check, per-device timing (CPU 0.011s, MPS 0.159s for 256x10000 round-trip). Did NOT edit operation/__init__.py or mapping.md and did NOT write report rows, per workflow overrides. |
| 2026-07-11 | operation.tanh_p1 | tanhP1 | ported | tests/operation/test_tanh_p1.py | bit-exact vs UnarySim tanhP1 (256 timesteps x 1000 elems, identical spike inputs, 0 mismatching timesteps) | Streaming unipolar series-expansion tanh (Parhi/Liu TETC 2017 fig.10). Reuses operation.dff for the 8-deep input and 3-deep n_1 decorrelation delay lines; four internal constant spike streams (62/153, 17/42, 2/5, 1/3) on sobol dims dim..dim+3, quantized as round(c*len) > floor(rng*len) to match UnarySim's SourceGen/BSGen exactly. hw_params(pp_delay=0) (combinational path; DFFs are decorrelators, not pipeline stages). Test asserts rmse < 0.1 vs torch.tanh (measured ~0.017 cpu / ~0.021 mps) plus tanh(0)=0 corner; test imports the class directly from napl.sim.operation.tanh_p1 since operation/__init__.py wiring is deferred to the workflow. UnarySim's unbounded bs_idx is replaced by modulo-len indexing, so agreement holds for timesteps <= 2**width (UnarySim itself OOBs beyond that). |
| 2026-07-11 | operation.tanh_pn | tanhPN | ported | tests/operation/test_tanh_pn.py | bit-exact (torch.equal) per-timestep spikes and decoded values vs UnarySim tanhPN on CPU and MPS (identical sobol streams, T=256, depth=3, 1000 elems); both show RMSE 0.2617 vs analytic tanh (inherent FSM approximation, not divergence) | FSM tanh(N*x/2), N=2**depth states (Brown & Card), bipolar rate-coded; modeled on relu_cnt sibling (scalar counter that broadcasts to input shape on first forward, hw_params(pp_delay=0)). Config key 'depth' (default 5; test uses 3). Test gates: RMSE vs analytic tanh(2**(depth-1)*x) < 0.35 (measured 0.2668, identical to UnarySim's inherent FSM approximation error) plus x=+/-1 saturation corners; x=0 is not a valid corner (deterministic 0.5-rate sobol stream parks the counter at threshold, identically in UnarySim). Per overrides: no reports/ recorder rows written, operation/__init__.py and mapping.md left untouched for the serialized wiring step. |
